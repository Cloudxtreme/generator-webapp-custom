language: node_js
node_js:
  - '0.8'
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-webapp-custom' ]; then cd .. && eval "mv $currentfolder generator-webapp-custom" && cd generator-webapp-custom; fi

